# 参与贡献

欢迎提交问题、改进建议和 Pull Request。眼镜操作、语音兼容性、游戏规则和文档都欢迎一起完善。

提交问题时，请说明版本、设备或浏览器、所在阶段、复现步骤和实际提示。
不要附上 API 密钥、管理口令、座位恢复凭证或未经同意的玩家聊天记录。

本地开发：

```sh
npm ci
npm test
python3 -m unittest discover -s test -p '*_test.py'
```

这些测试使用模拟模型，不需要 API 密钥。真实模型和设备联调方法见 [测试说明](docs/TESTING.md)。

修改眼镜客户端后，请重新构建并说明测试范围；模拟预览不能替代真机验证。
提交 PR 时简要说明问题、改动及验证结果。贡献代码采用本仓库的 MIT 协议。
